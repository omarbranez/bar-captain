<!DOCTYPE html>
<html>
<head>
<style>
img {
    width: 100%;
    height: auto;
    position: relative;
}
.card {
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
  max-width: 300px;
  margin: auto;
  text-align: center;
}

.title {
  color: grey;
  font-size: 18px;
}

button {
  border: none;
  outline: 0;
  display: inline-block;
  padding: 8px;
  color: white;
  background-color: #000;
  text-align: center;
  cursor: pointer;
  width: 100%;
  font-size: 18px;
}

a {
  text-decoration: none;
  font-size: 1em;
  color: black;
}

button:hover, a:hover {
  opacity: 0.7;
}
</style>
</head>
<body>

<div class="card">
  <div class="container">
  <h3><b><%= @product.name %></b></h3>
    <p><%= @product.subcategory %></p>
    <% if @product.description %>
        <h4>Description:</h4>
        <%= @product.description %></li>
    <% else %>
        <p><i>No description has been written for <%= @product.name %>.</i>
    <% end %>
    <p>This product is used to make the following drinks:</p>
    <% @drinks.each do |drink| %>
        <li> <a href="/drinks/<%= drink.drink.slug %>"><%= drink.drink.name %></a></li>
    <% end %>




  </div>
</div>
</body>
</html>



